import glob 
import os
import cv2 

LABEL_PATH = "/content/yolov5/runs/detect/exp2/labels"

def crop(path):
    img = os.path.basename(path)
    print(label)
    
    with open(f,'r') as fi:
        lines = fi.readlines()

if __name__ == "__main__":
    path = 
    crop(path)